apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "paperless.fullname" . }}-test-connection"
  labels:
    {{- include "paperless.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "paperless.fullname" . }}:{{ .Values.ports.http.port }}']
  restartPolicy: Never
